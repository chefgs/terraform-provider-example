package main

import (
	"testing"
)

// TestMain is a basic test to ensure the main package can be imported
func TestMain(t *testing.T) {
	// This test doesn't do much since main() just calls plugin.Serve()
	// But it ensures the main package can be imported without errors
}
